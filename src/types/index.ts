export type product = {
    id:number,
    name: string,
    price: number,
    category:string|null
}

export type user = {
    id:number,
    firstName:string,
    lastName:string ,
    password:string
}